import { ForfaitCanadaPipe } from './forfait-canada.pipe';

describe('ForfaitCanadaPipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitCanadaPipe();
    expect(pipe).toBeTruthy();
  });
});
