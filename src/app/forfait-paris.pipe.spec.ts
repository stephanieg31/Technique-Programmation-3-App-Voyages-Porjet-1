import { ForfaitParisPipe } from './forfait-paris.pipe';

describe('ForfaitParisPipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitParisPipe();
    expect(pipe).toBeTruthy();
  });
});
